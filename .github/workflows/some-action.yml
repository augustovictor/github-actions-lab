name: 'Some unimplemented action'

on:
  workflow_dispatch:

jobs:
  manual-deploy:
    runs-on: [ubuntu-latest]
    steps:
      - name: First step here
        run: |
          echo "Some commands here..."
      - name: Second step here
        run: |
          echo "Some commands here..."
      - name: Third step here
        run: |
          echo "Some commands here..."
